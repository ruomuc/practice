let turns = tasks.length
  // let idx = 0
  // let times = 0
  // while (turns) {
  //   const i = idx % 26
  //   if (countTask[i] == 0) {
  //     idx++
  //     continue
  //   }
  //   console.log(times, record[i], times - record[i], ans)
  //   if (countTask[i] > 0 && times - record[i] > n) {
  //     record[i] = times
  //     countTask[i]--
  //     ans++
  //   } else {
  //     ans += n
  //   }
  //   turns--
  //   times++
  //   idx++
  // }
  // return ans